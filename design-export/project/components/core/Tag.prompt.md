Small mono chip for genres and descriptors.

```jsx
<Tag>Hard Techno</Tag>
<Tag variant="accent">Industrial</Tag>
<Tag variant="solid" shape="pill">150–165 BPM</Tag>
```

`variant`: `outline | solid | accent`. `shape`: `square` (default) | `pill`.
