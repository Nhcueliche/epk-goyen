The GOYEN wordmark in Archivo Black. Clean for nav/flyers, glitch for covers.

```jsx
<Wordmark size={28} />
<Wordmark variant="glitch" size={120} live />
<Wordmark color="electric" size={40} />
```

`variant`: `clean | glitch`. `color`: `white | electric`. `size`: px number or CSS size.
